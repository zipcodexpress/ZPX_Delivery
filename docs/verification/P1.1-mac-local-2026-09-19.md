# P1.1 local PostgreSQL development verification

Purpose: record executed Mac/SSD setup evidence. Audience: developers.
Status: local development environment ready; P1.1 feature scope remains incomplete.
Owner: unassigned. Verified: 2026-09-19.

Checkout: `/Volumes/Document/Workspace/projects/Development/ZPX_Delivery`.
Branch: `feature/P1.1-postgresql-foundation`. GitHub CLI authentication verified.
The Document SSD is external writable APFS. Existing files were preserved.
Colima uses its existing SSD storage under `/Volumes/Document/Workspace/containers/colima`
and mounts this checkout writable. Local credentials are private and Git-ignored.

## Changes

The setup runner now queries the filesystem mount with `diskutil` instead of the
nested checkout directory, which this Mac rejects. HTTP readiness retries connections
closed during container startup. Both fixes have regression tests. No application
migrations or API contracts were changed. See [local setup](../LOCAL_DEVELOPMENT_MAC.md).

## Executed checks

- `python3 scripts/dev.py doctor`: passed with Colima.
- `python3 scripts/dev.py up`: passed on repeat startup; migrations repeat safely,
  PostgreSQL is healthy, and all six HTTP readiness checks passed.
- `python3 scripts/dev.py test-db`: passed real PostgreSQL integration checks,
  including schema inventory, checksum drift, transactional DDL rollback, foreign
  keys/uniqueness, runtime privileges, concurrent duplicate requests, outbox rollback,
  and sanitized readiness errors.
- `npm ci --ignore-scripts`: installed locked dependencies; audit reported zero vulnerabilities.
- `npm run check`: contract/type checks and all nine JavaScript tests passed.
- `npm run build`: both web applications built.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: six tests passed.
- `python3 docs/handoff/tests/validate_handoff.py`: passed static handoff checks.
- `git diff --check`: passed.

Native Node: 24.19.0; npm: 11.17.0; Python: 3.14.6; Compose: 5.5.1.
Container PostgreSQL: 17.11; PHP: 8.3.33. All three base images use arm64:

| Image | Observed digest |
|---|---|
| postgres:17-bookworm | sha256:639ab7ceb90e13123085b741fb31ef493fba25463002f6da665352e7b534b652 |
| php:8.3-cli-bookworm | sha256:013e2a0fab922e925d12c1546a58ab8ca4480ac22507894a1f18025707098414 |
| node:24.19.0-bookworm-slim | sha256:a9f5f7c91a432850b2a8a7797adf5eadb6c733ceed61167806cee7ea7fbc29df |

These are observed image identities, not new immutable Compose pins.
Compose used the classic builder because the optional buildx plugin is absent.

Services remain running: customer port 5173, operations 5174, API 8000, simulator
8090, all bound to loopback. PostgreSQL has no host port. No existing volumes were
deleted. Physical lockers, mobile applications, production integration and complete
parcel workflows were not tested. Remaining P1.1 feature work is documented in
[foundation evidence](P1.1-postgresql.md).
