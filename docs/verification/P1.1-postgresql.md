# P1.1 PostgreSQL foundation evidence

Purpose: distinguish implemented code from executed evidence. Audience: reviewers/developers.
Status: local Node/Python checks passed; real PostgreSQL/PHP execution pending CI at initial publication.
Owner: unassigned. Last reviewed: 2026-09-18. Related: issue 5.

Added 73-table PostgreSQL schema, checksum migration ledger, transactional migration runner, role provisioning, integrity constraints, history protection, PHP connection/transaction/outbox/HTTP layers and updated local startup. Historical MySQL files and volumes are preserved, not active application migrations.

Locally executed: `npm run check` (9 Node tests plus generated-contract/type checks), `npm run build` (both web applications), and `python3 -m unittest discover -s tests -p 'test_*.py'` (4 tests). All passed. PHP, PostgreSQL and Docker are absent in the editing workspace; no local SQL success is inferred from these results.

CI is configured to lint all PHP files, build the PHP/PostgreSQL image, initialize a real PostgreSQL 17 instance, run all migrations and repeat them, and execute database integration tests. Tests cover checksum drift, failed-DDL rollback, 74-table inventory, active-label/claim/idempotency uniqueness, malformed hashes and physical state, wrong package/session relationship, forbidden runtime DDL/history/ownership/ledger writes, two-process idempotency contention, transaction/outbox rollback and sanitized readiness failure. CI results will be recorded after execution.

Remaining P1.1: synthetic seed command, additional domain constraints/organization-scope tests, physical last-door reservation races and runtime services. Not claimed: ThinkPHP bootstrap, authentication, runtime OpenAPI payload validation, custody service, provider/worker implementation, M4/Android/hardware execution, or production deployment.
